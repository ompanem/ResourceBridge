# Public frontend config (safe to expose via VITE_ prefix)
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Server-only secrets (managed via Lovable Cloud, never in frontend code)
# LOVABLE_API_KEY — set in project secrets, used only in edge functions
# SUPABASE_SERVICE_ROLE_KEY — set in project secrets, never expose to client
